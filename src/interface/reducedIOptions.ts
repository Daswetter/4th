interface reducedIOptions {
  min?: number,
  max?: number,
  from?: number, 
  to?: number, 
  stepSize?: number,
  vertical?: boolean,
  double?: boolean,
  satellite?: boolean,
  scale?: boolean,
  progress?: boolean,
  input?: boolean
}

export { reducedIOptions }